# Contribuição

Crie uma branch por mudança e execute `npm run check` antes do pull request. Toda operação corporativa deve receber contexto de tenant validado, declarar sua permissão e incluir teste negativo entre tenants.
